[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Lokale Tests ohne laufenden Server (kein ENV erforderlich)
    live: Benötigt laufendes Backend (DONNA_TEST_URL + ADMIN_TOKEN)
    embedding: Benötigt numpy/sklearn für Cosine-Distanz-Metriken
